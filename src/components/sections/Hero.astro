---
import { Image } from "astro:assets";
import heroImage from "../../assets/hero_img.webp"

---

<section class="relative overflow-hidden bg-slate-50 py-16 lg:py-24">
    <div class="container mx-auto px-4 mt-8">
        <div class="flex flex-col lg:flex-row items-center gap-12">
            <div class="flex-1 text-center lg:text-left z-10 ">
                <span class="inline-block px-4 py-1.5 mb-6 text-sm font-semibold tracking-wider uppercase bg-blue-100 text-blue-700 rounded-full">Abierto 24hs.</span>
                <h1 class="text-4xl md:text-5xl lg:text-6xl font-extrabold text-slate-900 leading-tight mb-6">Cuidamos a quien más <span class="text-blue-600">queres</span>, cuando mas lo necesitas</h1>
                <p class="text-lg text-slate-600 mb-10 max-w-2xl mx-auto lg:mx-0">En Budhapets combinamos tecnología de vanguardia y calidez humana para brindar la mejor atención veterinaria.</p>
            </div>

            <div class="flex-1 relative">
                <div class="relative w-full max-w-lg mx-auto">
                    <div class="absolute -top-4 -right-4 w-72 bg-blue-200 rounded-full mix-blend-multiply filter blur-xl opacity-70"></div>
                    <div class="absolute -bottom-8 -left-4 w-72 h-72 bg-purple-200 rounded-full mix-blend-multiply filter blur-xl opacity-70 "></div>
                    <div class="relative">
                        <Image src={heroImage} alt="Mascota feliz en Budhapets" loading="eager" class="w-full h-auto object-cover" />
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

